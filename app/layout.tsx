import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Konsera - Semua Kebutuhan Konsermu dalam Satu Aplikasi",
  description: "Konsera adalah aplikasi all-in-one untuk pengalaman konser yang mudah dan berkesan. Temukan konser, beli tiket, gunakan fitur live crowd view, dan cari teman konser dengan Harmony Match.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
